version: "3"

services:
  web:
    build: 
      context: ./web
      args:
        NODE_ENV: production
    image: web
    container_name: web
    restart: always
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
    networks:
      - net
    volumes:
      - ./web:/usr/src/app
    tty: true
  db:
    build: ./database
    image: database
    environment:
      POSTGRES_PASSWORD: 1234
      POSTGRES_USER: postgres
      POSTGRES_DB: mydatabase
    container_name: database
    restart: always
    expose:
      - "5432"
    networks:
      - net
    tty: true

networks:
  net:
    driver: bridge
