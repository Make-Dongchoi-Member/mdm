{
  "name": "svelte-client",
  "version": "0.0.1",
  "scripts": {
    "dev": "vite dev --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch"
  },
  "devDependencies": {
    "@fontsource/fira-mono": "lastest",
    "@neoconfetti/svelte": "lastest",
    "@sveltejs/adapter-auto": "lastest",
    "@sveltejs/adapter-static": "lastest",
    "@sveltejs/kit": "lastest",
    "@types/cookie": "lastest",
    "svelte": "lastest",
    "svelte-check": "lastest",
    "tslib": "lastest",
    "typescript": "lastest",
    "vite": "lastest"
  },
  "dependencies": {
    "socket.io-client": "lastest"
  },
  "type": "module"
}
